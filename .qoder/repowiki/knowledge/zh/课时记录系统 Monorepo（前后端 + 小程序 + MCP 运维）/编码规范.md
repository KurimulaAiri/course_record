- API 路径必须带 Gateway 前缀 `/admin/`、`/biz/`、`/auth/`，禁止在前端或直连调用中省略
- 临时文件统一写入根目录 `.temp/`，不得散落在各子项目
- 后端新增功能遵循 Entity→DTO/VO→Converter→Mapper→Service(interface in common)→ServiceImpl(Controller only injects Service)