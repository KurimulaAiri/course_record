启动三种模式：`pnpm start`（stdio 默认）、`pnpm start:sse`（SSE 模式，端口由 `MCP_PORT` 控制）、`pnpm start:stdio`（显式 stdio）。SSE 模式需配置 `NACOS_URL`、`NACOS_USER`、`NACOS_PASSWORD`、`NACOS_NAMESPACE` 以便自动注册到 Nacos AI MCP；敏感凭据（JENKINS_TOKEN、DB_PASSWORD、BT_API_SK 等）通过 MCP 客户端注入的环境变量传入。